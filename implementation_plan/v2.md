# GenAI Guard Setup & Integration Plan

## Goal Description
The user wants to set up the GenAI Guard project, ensure the frontend integrates well with the backend (`audio_guard`, `file_guard`, `sandbox_integration`, `text_guard`, `web_guard`), and use provided models for fraud detection. We are preserving versioned plans in `implementation_plan/`.

## Proposed Changes

### 1. Fix Backend Main Application
#### [MODIFY] `app/main.py`(file:///d:/Project/GenAi-Guard/GenAi-Guard/app/main.py)
- Remove duplicate inclusion of `audio_routes` (lines 37 and 38).

### 2. Integrate Provided Models
- **Text Guard**: Integrate `tfidf_vectorizer.pkl` and `spam_classifier_model.pkl` (or `naive_bayes_model.pkl`).
#### [MODIFY] `app/modules/text_guard/routes.py`(file:///d:/Project/GenAi-Guard/GenAi-Guard/app/modules/text_guard/routes.py)
- Modify `analyze_text` to load the vectorizer, transform the text, and predict with the classifier for phishing checks. 
- **Web Guard**: Integrate `model.pkl` and feature extractors.
#### [MODIFY] `app/modules/web_guard/routes.py`(file:///d:/Project/GenAi-Guard/GenAi-Guard/app/modules/web_guard/routes.py)
- Add the necessary dependencies/imports for `ssl_check` and `typosquat`. Extract URL features (`URLLength`, `DomainLength`, `IsHTTPS`, `NoOfSubDomain`, `URLSimilarityIndex`) and pass them to the model prediction.

### 3. Missing Models Simulation
#### [NEW] `setup_dummy_models.py`(file:///d:/Project/GenAi-Guard/GenAi-Guard/setup_dummy_models.py)
- Generate a script to build dummy models for `injection_clf`, `voice_auth_model`, and `malware_sig_model` so the backend won't throw errors when they are called.

### 4. Frontend Integration Validation
- Validate the responses match what `app/simple_dashboard/script.js` expects.

## Verification Plan
1. Run `setup_dummy_models.py`
2. Start the FastAPI server using `python -m uvicorn app.main:app --host 0.0.0.0 --port 8000`.
3. Check `http://localhost:8000/dashboard`, upload texts and URLs to confirm predictions are working.
