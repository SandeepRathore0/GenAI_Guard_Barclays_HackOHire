# Gitignore Mapper

This document maps all the active `.gitignore` rules to their actual matching file or folder paths within the `GenAi-Guard-1` project directory. 

Root Directory: `C:\Users\sande\OneDrive\Desktop\New folder\New folder\GenAi-Guard-1`

## Environments
* **`venv/`**: `.\venv\` (Python virtual environment)
* **`env/`**: `.\env\` (Not currently present, reserved)
* **`.venv/`**: `.\.venv\` (Not currently present, reserved)
* **`.env`**:
  * `.\.env`
  * `.\services\llm_guard\.env`
* **`.env.local`**: `.\.env.local` (Not currently present)

## Python Cache and Compilation
* **`__pycache__/`**:
  * `.\app\__pycache__\`
  * `.\app\core\__pycache__\`
  * `.\app\modules\email_guard\__pycache__\`
  * And all other `__pycache__` folders generated by Python execution.
* **`*.pyc`, `*.pyo`, `*.pyd`**: Compiled Python bytecode inside `__pycache__` folders.
* **`.pytest_cache/`**: Generated when running tests via Pytest.

## IDE and OS
* **`.vscode/`**: `.\.vscode\` (VS Code local workspace settings)
* **`.idea/`**: `.\.idea\` (IntelliJ/PyCharm settings)
* **`.DS_Store`**: Hidden macOS metadata files (if accessed from Mac).
* **`Thumbs.db`**: Windows thumbnail cache files.
* **`*.swp`**: Vim or other editor swap files.

## Logs and Local Data
* **`*.log`**: matches all log files.
  * `.\error.log`
  * `.\error_clean.log`
  * `.\services\audio_guard\error.log` (if any are generated)
* **`activity_log.json`**: `.\activity_log.json`
* **`error.log`**: `.\error.log` (Explicitly tracked)
* **`error_clean.log`**: `.\error_clean.log` (Explicitly tracked)

## Temporary / Build Files
* **`.tmp/`**, **`tmp/`**: Temporary processing folders.
* **`*.sqlite3`**: Local SQLite databases if generated.
* **`.coverage`**, **`htmlcov/`**: Test coverage reporting artifacts.
* **`build/`**, **`dist/`**, **`*.egg-info/`**: Python packaging build artifacts.

## ML Models and Large Artifacts
* **`*.pkl`, `*.pth`, `*.h5`, `*.bin`, `*.joblib`, `*.model`, `*.onnx`**: Machine learning model weights generated through training scripts (`train_advanced_models.py`, `train_media_models.py`), ensuring they are not committed regardless of location.

## Ignore specific model directories
* **`**/models/` & `models/`**:
  * `.\app\modules\text_guard\models\`
  * `.\app\modules\web_guard\models\`
  * `.\app\modules\file_guard\models\`
  * `.\services\audio_guard\core\models\`
* **`services/audio_guard/core/output/`**: `.\services\audio_guard\core\output\` (Contains `2_best_model_FineTuned.pth` and other deep learning checkpoints)

## API Credentials and Secrets
* **`credentials.json`**: `.\app\modules\email_guard\credentials.json` (Google Gmail API OAuth credentials)
* **`token.json`**: `.\app\modules\email_guard\token.json` (Google Gmail API refresh token)

## Additional Large Extensions
* **`*.safetensors`**:
  * `.\services\audio_guard\core\models\wavlm-base\model.safetensors`
